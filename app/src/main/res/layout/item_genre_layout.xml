<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/genreNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Ужасы"
        android:paddingHorizontal="@dimen/genre_padding_horizontal"
        android:paddingVertical="@dimen/genre_padding_vertical"
        android:textColor="@color/white"
        android:textSize="@dimen/genre_text_size"
        android:background="@drawable/bg_genre"
        android:layout_marginBottom="@dimen/spacer"
        android:layout_marginStart="@dimen/spacer"/>

</androidx.constraintlayout.widget.ConstraintLayout>